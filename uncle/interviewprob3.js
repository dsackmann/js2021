// node spawnJobs.js 100
const job_count = process.argv[2]
var i = 0;
function spawnJob(){
  //spawnJob code
}
while (i < job_count) {
  spawnJob(i);
  i++;
}
console.log(job_count+"jobs have been spawned.");